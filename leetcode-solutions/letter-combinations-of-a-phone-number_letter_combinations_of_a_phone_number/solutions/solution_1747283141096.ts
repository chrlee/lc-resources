const dArr = digits.split('');    const s: [string, number][] = numToCharMap[+dArr[0]].map((v) => ([v, 0]))    while(s.length){    if(!digits.length) return out;        const curr = s.pop();        if(curr[1] === digits.length-1){            out.push([...letters, curr[0]].join(''));        }        letters.push(curr[0]);        const next: [string, number][] = numToCharMap[+dArr[curr[1]+1]]?.map((v) => [v, curr[1]+1]);        if(next) s.push(...next);    }    const letters = [];    const out = [];        while(+curr[1] < letters.length){            letters.pop();        }    return out;};