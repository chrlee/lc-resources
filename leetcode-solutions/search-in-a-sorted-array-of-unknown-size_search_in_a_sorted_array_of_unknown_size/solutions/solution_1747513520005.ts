function search(reader: ArrayReader, target: number): number {const MAX_VAL = 10000;    let [l, r] = [0, MAX_VAL];    while(l <= r){        const mid = Math.floor((l+r)/2);        if(reader.get(mid) === target) return mid;        [l, r] = reader.get(mid) > target ? [l, mid-1] : [mid+1, r];    }    return -1; *      // You should not implement it, or speculate about its implementation *      get(index: number): number {}; *  }; */ *      // This is the ArrayReader's API interface. * class ArrayReader {/**};