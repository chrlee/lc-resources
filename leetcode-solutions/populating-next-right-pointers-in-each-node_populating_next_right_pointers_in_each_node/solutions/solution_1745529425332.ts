for(let i = 0; i < level; ++i){            const curr: _Node = q.shift()            if(curr.left) q.push(curr.left);            if(curr.right) q.push(curr.right);            curr.next = i === level-1 ? null : q.at(0);        }        const level = q.length;    while(q.length){    let q: _Node[] = [root];    if(!root) return root;    }    return root;};