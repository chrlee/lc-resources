for(let i = 0; i < level; ++i){            const curr: _Node = q.at(i)            if(curr.left) q.push(curr.left);            if(curr.right) q.push(curr.right);            curr.next = i === level-1 ? null : q.at(i+1);        }        const level = q.length;    }    while(q.length){    let q: _Node[] = [root];    if(!root) return root;        q = q.slice(level, q.length);    return root;