function hasPathSum(root: TreeNode | null, targetSum: number): boolean {    const pathHelper = (curr: TreeNode | null, path: number) => {        if(curr.val + path === targetSum && !curr.right && !curr.left) return true;    };    if(!root) return false;        const inLeft = curr.left ? pathHelper(curr.left, curr.val + path) : false;        const inRight = curr.right ? pathHelper(curr.right, curr.val + path) : false;        return inLeft || inRight;    return pathHelper(root, 0);};