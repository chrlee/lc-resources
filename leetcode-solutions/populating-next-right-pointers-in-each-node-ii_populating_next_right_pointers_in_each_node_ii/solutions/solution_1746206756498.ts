if(!root) return root;    const q = [root];    while(q.length){        const lvlLength = q.length;        for(let i = 0; i < lvlLength; ++i){            const curr = q.shift();            curr.next = i === lvlLength - 1 ? null : q.at(0);            if(curr.left) q.push(curr.left);            if(curr.right) q.push(curr.right);        }    }function connect(root: _Node | null): _Node | null {