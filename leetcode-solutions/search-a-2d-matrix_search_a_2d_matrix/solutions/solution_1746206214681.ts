let [l,r] = [0, matrix[row].length-1];    const row = getRow()-1;    if(row === -2) return true;    while(l<=r){    }        return l === 0 ? 1 : l;        }        const mid = (l+r)>>>1;            [l,r] = matrix[mid][0] < target ? [mid+1,r] : [l,mid-1];        if(matrix[row][mid] === target) return true;        [l,r] = matrix[row][mid] < target ? [mid+1,r] : [l,mid-1];    }    return false;