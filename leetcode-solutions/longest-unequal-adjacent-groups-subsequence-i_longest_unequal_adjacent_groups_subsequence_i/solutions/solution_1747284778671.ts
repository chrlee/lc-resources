function getLongestSubsequence(words: string[], groups: number[]): string[] {    let max = 0;    for(let i = 0; i < groups.length; ++i){        for(let j = i+1; j < groups.length; ++j){            if(groups[j] !== groups[nums.at(-1)]) nums.push(j);        }    }    return maxOut.map((v) => words[v])    let maxOut = [];        const nums = [i];        if(nums.length > max){            max = nums.length;        }            maxOut = nums;};